apiVersion: v1
kind: Service
metadata:
  name: github-mcp-server
  labels:
    app: github-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  selector:
    app: github-mcp-server
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  sessionAffinity: None
