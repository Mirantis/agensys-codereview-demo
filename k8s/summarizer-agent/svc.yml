apiVersion: v1
kind: Service
metadata:
  name: summarizer-agent
  namespace: agensys-demo-1
  labels:
    app: summarizer-agent
    component: agent
spec:
  type: ClusterIP
  selector:
    app: summarizer-agent
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  sessionAffinity: None
