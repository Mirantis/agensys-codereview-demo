apiVersion: v1
kind: Service
metadata:
  name: pr-agent
  namespace: agensys-demo-1
  labels:
    app: pr-agent
    component: agent
spec:
  type: ClusterIP
  selector:
    app: pr-agent
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  sessionAffinity: None
